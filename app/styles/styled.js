import styled from 'styled-components';

import { Anchor } from 'antd';

export const StyledAnchor = styled(Anchor)`

  .ant-anchor-link-ball.visible {
    display: none;
  }
  div>.ant-anchor-link:first-child{
    padding-left: 0
  }
`;